apiVersion: api.cerbos.dev/v1
resourcePolicy:
  version: "default"
  resource: message_template
  rules:
    - actions: ["view"]
      roles:
        - admin
      effect: EFFECT_ALLOW
    - actions: ["create"]
      roles:
        - admin
      effect: EFFECT_ALLOW