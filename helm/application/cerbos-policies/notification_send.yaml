apiVersion: api.cerbos.dev/v1
resourcePolicy:
  version: "default"
  resource: message
  rules:
    - actions: [ "view" ]
      roles:
        - notification-admin
    - actions: ["send"]
      roles:
        - notification-send
      effect: EFFECT_ALLOW